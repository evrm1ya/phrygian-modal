{
  "name": "phrygian-modal",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "babel-modalizr": "babel lib/modalizr --out-dir compiled --source-maps",
    "babel-src": "babel src --out-dir compiled --source-maps",
    "browserify-modal-app": "browserify ./compiled/modal-app.js -o ./main.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.6.0",
    "babel-preset-es2015": "^6.6.0",
    "babelify": "^7.2.0",
    "browserify": "^13.0.0"
  }
}
